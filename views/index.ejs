<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Custom Tokenizor</title>
    <style>
      * {
        padding: 0;
        margin: 0;
      }

      body {
        font-family: Arial, Helvetica, sans-serif;
        /* background: linear-gradient(#9494d8, #3939ce); */
        background: linear-gradient(#9198e5);
      }

      h1 {
        text-align: center;
        margin-top: 2rem;
        font-weight: 700;
        font-size: 3rem;
        color: #080d49;
      }

      .container {
        padding: 2rem;
        display: flex;
        justify-content: space-around;
        box-sizing: border-box;
        width: 99vw;
      }

      .box {
        width: 45%;
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
        border: 1px solid #292f73;
        border-radius: 1rem;
        box-sizing: border-box;
        padding: 1rem;
      }

      form {
        width: 100%;
        display: flex;
        flex-direction: column;
        gap: 1rem;
      }

      input[type="text"] {
        width: 90%;
        padding: 8px;
      }

      button {
        padding: 8px 15px;
        margin-left: 10px;
        border-radius: 8px;
        width: 35%;
      }

      .text {
        padding: 8px;
        background-color: aliceblue;
        border: 1px solid #282a3f;
        border-radius: 8px;
      }
    </style>
  </head>
  <body>
    <h1>Token Encoder and Decoder</h1>
    <div class="container">
      <div class="box">
        <form action="/encode" method="post">
          <h2>Encode Input</h2>
          <div>
            <input
              type="text"
              name="user_input"
              placeholder="Enter anything"
              value="<%= input ? input : '' %>"
            />
            <% if (error1) {%>
            <p style="color: red; font-size: 12px"><%=error1%></p>
            <% } %>
          </div>
          <button>Tokenize</button>
        </form>
        <% if (encodedToken) { %>
        <div class="result">
          <p style="font-weight: 700; font-size: 1.25rem">Encoded Token:</p>
          <p class="text">[<%= encodedToken.join(", ") %>]</p>
        </div>
        <% } %>
      </div>

      <div class="box">
        <form action="/decode" method="post">
          <h2>Decode Token</h2>
          <div>
            <input
              type="text"
              name="token_input"
              placeholder="Enter your tokenize text"
              value="<%= decodedInput ? decodedInput : '' %>"
            />
            <% if (error2) {%>
            <p style="color: red; font-size: 12px"><%=error2%></p>
            <% } %>
          </div>
          <button>Decoded</button>
        </form>
        <% if (decodedFromToken) { %>
        <div class="result">
          <p style="font-weight: 700; font-size: 1.25rem">Decoded Text:</p>
          <p class="text"><%= decodedFromToken %></p>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
